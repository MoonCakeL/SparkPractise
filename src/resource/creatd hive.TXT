create table HI_data1 (
REGION STRING,
MACHINEROOM STRING,
CIP STRING,
CHASSIS STRING,
SLOT_SN STRING,
SLOT_TYPE STRING,
PORT_SN STRING,
PORT_TYPE STRING,
MODULE STRING,
MODE STRING,
LOS STRING,
LOF STRING,
OOF STRING,
ENABLE STRING)
ROW FORMAT DELIMITED FIELDS TERMINATED BY ','
stored as textfile;

load data inpath '/hivedata/data' into table HI_data1;