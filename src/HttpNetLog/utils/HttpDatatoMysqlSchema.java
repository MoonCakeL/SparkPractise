package HttpNetLog.utils;

import org.apache.spark.sql.Dataset;
import org.apache.spark.sql.Row;
import org.slf4j.Logger;
import org.slf4j.LoggerFactory;

public class HttpDatatoMysqlSchema {
	private static final Logger logger = LoggerFactory.getLogger(HttpDatatoMysqlSchema.class);

	public void save(Dataset<Row> rowDataset,String tableName){

	}

	public void createTable(String tableName){


	}
}
